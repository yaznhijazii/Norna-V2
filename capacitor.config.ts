import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
    appId: 'com.yaznhijazi.norna',
    appName: 'Norna',
    webDir: 'dist',
    server: {
        androidScheme: 'https'
    }
};

export default config;
