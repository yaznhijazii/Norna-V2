# 🕌 إعداد Supabase لتطبيق نورنا

## خطوات الإعداد:

### 1️⃣ إنشاء مشروع Supabase جديد
1. اذهب إلى [supabase.com](https://supabase.com)
2. أنشئ مشروع جديد
3. انتظر حتى يكتمل الإعداد

### 2️⃣ تشغيل الـ SQL Script
1. في لوحة التحكم، اذهب إلى **SQL Editor**
2. انسخ محتويات ملف `/supabase-step-by-step.sql` كاملاً
3. الصقها في SQL Editor
4. اضغط **Run** أو **F5**
5. تأكد من ظهور رسالة: `All tables created successfully!`

### 3️⃣ الحصول على مفاتيح الاتصال
1. اذهب إلى **Project Settings** > **API**
2. انسخ:
   - **Project URL** (مثال: `https://xxxxx.supabase.co`)
   - **anon public key** (المفتاح العام)

### 4️⃣ ربط التطبيق بـ Supabase
1. في التطبيق، اضغط على أيقونة الإعدادات ⚙️
2. اختر "Connect Supabase"
3. الصق:
   - Supabase URL
   - Supabase Anon Key
4. اضغط "Save"

### 5️⃣ التحقق من الاتصال
1. سجّل دخول أو أنشئ حساب جديد
2. جرّب:
   - ✓ علّم صلاة
   - ✓ افتح أذكار الصباح
   - ✓ احفظ دعاء
3. إذا عملت كلها، يعني الاتصال تمام! ✅

---

## 📋 الجداول المُنشأة:

- ✅ `users` - المستخدمين
- ✅ `user_sessions` - الجلسات
- ✅ `prayers` - الصلوات
- ✅ `athkar` - الأذكار
- ✅ `quran_readings` - قراءة القرآن
- ✅ `duaas` - الدعاء
- ✅ `activities` - النشاطات (Timeline)

---

## ⚠️ ملاحظات مهمة:

1. **لا تشارك مفاتيح Supabase مع أحد!**
2. الـ Row Level Security (RLS) مفعّل بسياسات عامة - يمكنك تخصيصها لاحقاً
3. كل البيانات محفوظة في Supabase وليس localStorage
4. عند الربط مع الشريك، تأكد من أن كلاكما متصل بنفس قاعدة البيانات

---

## 🔧 استكشاف الأخطاء:

### "Could not find the table..."
- تأكد من تشغيل الـ SQL script كاملاً
- جرب تحديث Schema Cache: **Database** > **Replication** > **Reload schema cache**

### "column does not exist"
- تأكد من استخدام أحدث نسخة من `/supabase-step-by-step.sql`
- جرب مسح الجداول وإعادة إنشائها

### "Network error" أو "Failed to fetch"
- تأكد من أن Supabase URL صحيح
- تأكد من اتصال الإنترنت

---

**بالتوفيق! 🌙✨**
